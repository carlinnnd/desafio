#include <iostream>
#include <string>

using namespace std;

int main() {
    string n, x;

    cout << "Escreva uma string (palavra ou frase): ";
    getline(cin, n);

    for (int i = n.size() - 1; i >= 0; i--) {
         x += n[i];
    }
    cout << x;
   
    
    return 0;
}
